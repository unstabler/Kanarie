#include "accounts.hpp"

namespace KanarieUI 
{
    namespace Settings 
    {
        Accounts::Accounts(QWidget *parent) 
            : QWidget(parent) 
        {
            ui.setupUi(this);
        }
    }
}
